
import { Meta, StoryObj } from '@storybook/react';
import { SeatItem } from './SeatItem'

export default {
  title: 'Components/SeatItem',
  component: SeatItem,
} as Meta

export const Default: StoryObj = {}